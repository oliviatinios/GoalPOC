package net.chrisrichardson.eventstore.examples.todolist.model;

import com.cibc.dgp.api.client.model.Client;
import com.cibc.dgp.api.goal.model.Goal;
import com.cibc.dgp.api.goal.model.SaveForAnythingDetails;

public class TestDGPGoal {

	public TestDGPGoal() {
		com.cibc.dgp.api.goal.model.Goal goal = null;
		/*goal.setCreatedBy(createdBy);//string
		goal.setCreatedOn(createdOn);//string
		goal.setGoalId(goalId);//long
		goal.setGoalName(goalName);;
		goal.setGoalStatusCode(goalStatusCode);//enum
		goal.setGoalSubTypeCode(goalSubTypeCode);//string
		goal.getGoalTypeCode();//string
		goal.setModifiedBy(modifiedBy);//string
		goal.setModifiedOn(modifiedOn);//string
		goal.setNote(note);//String
		
		Client client;
		client.setClientId(clientId);//long
		client.setClientTypeCode(clientTypeCode);//
		client.setClientXRefId(clientXRefId);
		client.setCreatedBy(createdBy);
		client.setCreatedOn(createdOn);
		client.setLastModifiedBy(lastModifiedBy);
		client.setLastModiefiedOn(lastModifiedOn);
		
		goal.setPrimaryClient(client);//Client
		
		SaveForAnythingDetails saveforAnythignDetails;
		saveforAnythignDetails.setCompleteDate(completeDate);//String
		saveforAnythignDetails.setCost(cost);//String
		saveforAnythignDetails.setCostFrequencyCode(costFrequencyCode);//costFrequencyCode
		saveforAnythignDetails.setCostTimeValue(costTimeValue);//enum
		saveforAnythignDetails.setCurrencyCode(currencyCode);//enum
		saveforAnythignDetails.setInvestmentPropertyInd(investmentPropertyInd);//
		saveforAnythignDetails.setOwnedHomeCanadaInd(ownedHomeCanadaInd);//enum
		saveforAnythignDetails.setRecurrence(recurrence);//int
		saveforAnythignDetails.setStartDate(startDate);
		saveforAnythignDetails.setTargetDateCode(targetDateCode);//enum
		
		goal.setSaveForAnythingDetails(saveforAnythignDetails);//saveforAnythignDetails*/
		
	}

}
